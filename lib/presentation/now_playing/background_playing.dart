import 'package:flutter/material.dart';

class BackgroundPlaying extends StatelessWidget {
  const BackgroundPlaying({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
